#pragma once

enum BossPage { PAGE_EMPLOYEES = 0, PAGE_PRODUCTS, PAGE_SALES, PAGE_STATISTIC, PAGE_SALARY };

/**
 * Перечисление основных виджетов программы
 */
enum ProgramWidgets { AUTH_WIDGET = 0, PROGRAM_WIDGET = 1 };

/**
 * Перечисление основных виджетов по ролям
 */
enum UserRoleWidgets { BOSS_WIDGET = 0, MANAGER_WIDGET = 1 };